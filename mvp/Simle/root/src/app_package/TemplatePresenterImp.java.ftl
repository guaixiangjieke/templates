package ${TempPackageName};

/**
 * ${templateDescribe} mvp presenter实现类
 */

public class ${TempPresenterName} extends BasicMvpPresenter<Contract.IModel, Contract.IView> implements Contract.IPresenter {
    public ${TempPresenterName}(Contract.IModel model, Contract.IView view) {
        super(model, view);
    }
}
